<?PHP

/*INSERT INTO ptf_players_squad (ID, squadTeam, PlayerID, TeamSlot)
SELECT
    (@id := @id + 1) AS ID,
    squadTeam,
    0 AS PlayerID,
    teamSlot
FROM
    (SELECT 1 AS squadTeam UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5
     UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9 UNION SELECT 10
     UNION SELECT 11 UNION SELECT 12 UNION SELECT 13 UNION SELECT 14 UNION SELECT 15
     UNION SELECT 16 UNION SELECT 17 UNION SELECT 18 UNION SELECT 19 UNION SELECT 20) AS squads
CROSS JOIN
    (SELECT 1 AS teamSlot UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7) AS slots,
    (SELECT @id := 0) AS init;
    
    */

?>



